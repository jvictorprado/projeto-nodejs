pipeline {
    agent any

    stages {
        stage('Run') {
            steps {
                // Este estágio inicia o servidor da aplicação
                sh 'node app.js'
	    }
    	}
    }
}
